function twoSum(arr, target) {
  let map = {}; // stores number → index

  for (let i = 0; i < arr.length; i++) {
    let diff = target - arr[i];

    if (map[diff] !== undefined) {
      return [map[diff], i];
    }

    map[arr[i]] = i;
  }
}

console.log(twoSum([2, 7, 11, 15], 9)); // [0,1]
